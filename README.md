① 「MAXを使う条件」を自分ルール化する（最重要）

まずこれができてないと、全部がブレます。

✔ MAX を使う条件（YES / NO）

MAX ON にする

新規プロジェクト作成

DB / RLS / 権限設計

既存設計に疑問があるとき

原因不明のバグ

複数ファイルにまたがる修正

パフォーマンス・整合性問題

MAX OFF

UI調整

文言変更

単純CRUD

1ファイル完結の処理

型補完・リネーム

👉
「設計に触るか？」だけで判断
これが一番シンプルで強いです。

② 「いきなりコードを書くな」と指示する

Cursorは放っておくと
👉 即コードを書き始める 癖があります。

毎回これを最初に入れるだけで精度が激変します
いきなり実装せず、
まず設計・影響範囲・懸念点を整理してください。


MAX時は特に重要。

③ 差分前提で指示する（修正効率UP）

Cursorにありがちな失敗：

不要なリファクタ

勝手な最適化

コード全体を書き換える

防止テンプレ
既存実装を壊さず、
必要最小限の差分で修正してください。


これだけで
👉 **「シニアっぽい修正」**になります。

④ コンテキストは「読ませすぎない」

Cursorは賢いですが、
読ませすぎると逆に精度が落ちます。

悪い例

プロジェクト全体を毎回選択

関係ないファイルまで含める

良い例

関係ファイルだけ選択

DB設計なら migration + schema

UIなら component + hook だけ

👉
人間が読む範囲と同じでOK

⑤ 「決めてほしいのか」「実装してほしいのか」を明示する

Cursorはこれを区別できません。

指示の型を分ける

判断させたい時

この実装方針は設計的に正しいですか？
代替案があれば理由付きで教えてください。


実装させたい時

この方針で確定なので、
実装してください。


👉 混ぜると精度が落ちます。

⑥ .cursor/rules を最低限入れる（効果大）

設定で一番コスパが良いのはこれ。

最低限おすすめ
- 既存コードを尊重する
- 不明点は仮定を明示
- 差分で修正
- Supabase RLS / migration 前提
- 不要なリファクタは禁止


これで 回答のブレが激減します。

⑦ 「Cursorは実装係」と割り切る（重要）

一番うまく使っている人の共通点です。

仕様整理・要件定義 → 人 or ChatGPT

設計レビュー → Cursor MAX

実装 → Cursor 通常

判断が必要 → MAX

👉
Cursorに丸投げしない方が、結果的に賢く見える

⑧ 1タスク1プロンプトを守る

Cursorは

複数要求

曖昧なゴール

があると暴走します。

悪い例

バグ直して最適化して設計も見直して

良い例

このバグの原因を特定してください
→ OK
修正案をください
→ OK
実装してください
→ OK

⑨ 「なぜそうしたか」を必ず聞く

これをやると、
Cursorが急に慎重になります。

なぜこの実装にしたのか説明してください。


→ 変な実装は自分で引っ込めることが多いです。

⑩ 効率が落ちてきたら MAX を切る

地味ですが重要。

応答が浅い

同じ説明を繰り返す

文脈を忘れる

こうなったら
👉 MAX OFF + 新しいプロンプト
の方が速いです。
